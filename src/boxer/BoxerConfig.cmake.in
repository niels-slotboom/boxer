@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find transitivity dependencies required by target_link_libraries(PUBLIC ...)
find_dependency(Qt6 COMPONENTS Widgets OpenGLWidgets REQUIRED)

# Include exported targets
include("${CMAKE_CURRENT_LIST_DIR}/BoxerTargets.cmake")